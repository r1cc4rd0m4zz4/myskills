[project]
name = "secure-url-fetcher"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "urllib3>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
verbosity = 2

[tool.uv]
offline = true
