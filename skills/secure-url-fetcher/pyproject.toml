[project]
name = "secure-url-fetcher"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "urllib3==2.2.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
verbosity = 2
